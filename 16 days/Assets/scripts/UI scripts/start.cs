using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class start : MonoBehaviour
{

    public bool mapstart = false;
    // Start is called before the first frame update
    void Start()
    {
        mapstart = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
