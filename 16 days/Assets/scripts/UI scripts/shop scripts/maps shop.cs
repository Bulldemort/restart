using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mapsshop : MonoBehaviour
{
    public bool mapshop = false;
    // Start is called before the first frame update
    void Start()
    {
        mapshop = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
